CREATE TABLE Book_Ms_Entity (
    isbn Integer AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255),
    published_Date VARCHAR(255),
    total_Copies INTEGER,
    issued_Copies Integer,
    author Varchar(255)
);
